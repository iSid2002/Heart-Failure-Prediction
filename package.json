{
  "name": "heart-disease-prediction",
  "version": "1.0.0",
  "description": "Heart Disease Risk Prediction Application",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r frontend/* dist/",
    "deploy": "npm run build && npm start",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": "18.x"
  }
} 